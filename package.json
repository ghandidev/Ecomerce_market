{
  "dependencies": {
    "sass": "1.77.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass",
    "sass:watch": "sass --watch sass/styles.scss sass/main.css"
  }
}
